/** @jsx dom */
var dom = React.createElement;

var foo = 'bar';

ReactDOM.render(
    ::`
    div
        for i in range(1, 6)
            .row
                for i in range(1, 10)
                    .tile`,
    document.getElementById('map')
);
